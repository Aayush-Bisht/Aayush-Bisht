// Online C compiler to run C program online
#include <stdio.h>
#include <math.h>

int main() 
{
    double num;
printf("Enter a Number:");
scanf("%lf",&num);

  printf("%.2lf\n",sqrt(num));
  printf("%.2lf",pow(num,sqrt(num)));
    return 0;
}
