// Number and Power Gave by the user
#include <stdio.h>
#include <math.h>
int main() {
    double a,b;
    printf("Enter a Number:");
    scanf("%lf",&a);
    
    printf("Enter a Power:");
    scanf("%lf",&b);
    
    printf("%.2lf",pow(a,b));
    
    return 0;
}
